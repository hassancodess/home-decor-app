import { Input as _Input, styled } from 'tamagui';

const Input = styled(_Input, {
  padding: '$0',
  flex: 1,
  autoCapitalize: 'none',
  autoCorrect: false,
  backgroundColor: '$beige',
  borderWidth: '$0',
  fontWeight: '$2',
  color: '$black',
});
export default Input;
